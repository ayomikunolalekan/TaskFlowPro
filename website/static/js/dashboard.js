// // script.js

// document.addEventListener('DOMContentLoaded', function () {
//   // Wait for the DOM content to be fully loaded

//   // Create a new i element with Font Awesome classes
//   var barsIcon = document.createElement('i');
//   barsIcon.className = 'fas fa-bars';

//   // Get the label element by its 'for' attribute
//   var labelElement = document.querySelector('label[for="check"]');

//   // Append the Font Awesome icon inside the label
//   labelElement.appendChild(barsIcon);
// });
